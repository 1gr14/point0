{
  "name": "@point0/test-project-name",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "point0 dev --env NODE_ENV=development",
    "generate": "point0 generate",
    "build": "point0 build --env NODE_ENV=production",
    "start": "bun run ./dist/server/index.server.js",
    "clean": "rimraf dist",
    "types": "tsc --noEmit -p tsconfig.build.json",
    "typesgo": "tsgo --noEmit -p tsconfig.build.json"
  },
  "dependencies": {
    "@point0/core": "workspace:*",
    "@point0/engine": "workspace:*",
    "@point0/wouter": "workspace:*",
    "@point0/react-dom": "workspace:*",
    "@point0/compiler": "workspace:*",
    "@devp0nt/route0": "^1.0.0-next.55",
    "superjson": "^2.2.5",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "server-only": "^0.0.1"
  },
  "devDependencies": {
    "@svgr/core": "^8.1.0",
    "@svgr/plugin-jsx": "^8.1.0",
    "@types/bun": "^1.3.0",
    "@types/react": "^19.2.2",
    "@types/react-dom": "^19.2.2",
    "cross-env": "^10.0.0",
    "ncp": "^2.0.0",
    "rimraf": "^6.0.1",
    "typescript": "^5.9.3"
  }
}
