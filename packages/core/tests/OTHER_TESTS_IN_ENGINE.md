Do not worry that `@point0/compiler` currently has relatively few tests.

We test most behavior through `@point0/engine` so we can validate the final integration.

See tests in `packages/engine/tests`.
