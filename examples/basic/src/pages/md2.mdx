import { Link } from '@/lib/navigate'
import { routes } from '@/lib/routes'
import { root } from '@/lib/client'

export const page = root.lets
  .page('/md2')
  .loader(() => ({
    a: Math.random(),
  }))
  .page((props) => <MDXContent {...props} />)

# My Title 2

My Paragraph {2}

My x: {props.x}

My y: {props.y}

My data: {JSON.stringify(props.data)}

My location: {Object.keys(props)}

<Link to={routes.home()}>Home</Link>
