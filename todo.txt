server entry settings ssr and stanalone different
for ssr {client:{},server:{}}

client/hydrate, client/components/ErrorBoundary, client/components/ErrorPage
<App /> becouse it can be client only app and we can figure out how work with it
error handling, just catch it in <App><ErrorBoundary></App>, or better .errorComponent()
.route(string), points collection also [string, import]
.ctx({}) just append, not replace

.page(() => [201, <div />]).page(() => [404, <div />])

inject react query, queryClient to Point0.client({ queryClient })
const {data, meta, res} = await somePage.fetch({ id: 123 }) → json
on client before calling .page() we should useReactQuery(route{saerchParams, headers})
.page(queryOptions, () => <div />) to provide options for this query
.input<T>({params:...,query:...,body:...,headers:...}) or .params<T>().query().body().headers() or .query(zodScema) or .query(anyOtherSchema), .output({}).status()
.endpoint() → just another loader but should be last

.before, .after

auth

.exposeCtx, add value to __PAGE0_EXPOSED_CTX__ as usual javascript, it can even have await import('...') inside. Or maybe it returns not result, but array of extend fns itself, to be called in client right before calling .clientCtx fns
.uesExposedCtx
.clientLoader, after call do not allow usual loader another
.clientCtx, has access to exposeCtx,  just add  and there real javascript, can have usual useHooks (useEffect etc) will be called only on client

react router helper
expo router helper
.loaderComponent
client only page
client only routing


.route( * will be used as is in fetch request ).id().loader().layout(() = <>{children}</>) standalone
.loader().id().layout(() = <>{children}</>).route(here id resets to route path).loader().page() connected to page
find suiatble layouts, find suitable page. Extract each layout payload, extract page payload. Render <Layout1 data={data1}><Layout2 data={data2}><Page data={data3} /></></>. __PAYLOAD_0__=data1 __PAYLOAD_1__=data2 __PAYLOAD_2__
layoutsCollection
pagesCollection
endpointsCollection
layout scroll position ok
example general layout, idea layout, idea news page

createBunClient({ErrorBoundary, ErrorPageComponent})
createBunServer
entryInput should be gettable from client and server itslef
we alway should pass client, but we can pass server as client. {server, client: server}
config for generation points file
config for generation server entry, client entry, ot at least forward settings to them
env POINT0_SERVER_PUBLIC_DIR, POINT0_... to override config props
cli for simplify commands, like build with bun: point0 build
points collection generator, auto find points [Page$, Endpoint$], sort by no params to params routes, save to file: version with text anyze, version with parse code on exports with .page() or .endpoint() in the end
.disable()

extendFns should store middle state (extractedExtendFsn) and be returned on after extract, and can be passed to another extract to avoid recalling we can store it in {cache}
const {ctx, data, meta} = await somePage.extract({ctx: currentCtx, data: currentData, meta: currentMeta}) // inside another loaders or ctxFns
const plugx = createPlugin(...), point.use(plugx(...)), Point0.use(plugx) → point.plugx(....)

.loader(...).component(() => </div>) return usual ReactElemnt with property "loader" so on server rendering we can see that he has it and call its loaders having cahe of previously called extendFns

example expo
example vite-hono
example elysia

meta, title
test for units
html template parsing just by provided data from endpoint or force provided vars
test for server
server0.routes({})


split index.ts code
htmlRewriter fall back on no bun
hide methods after .route() called, etc (maybe add pageCanNotBeSetWithoutRote, maybe add deprecation flags)
Error0 as serializeError and deserializeError provider
superjson
on client build remove server code, just force to rebuild @point0/core package and there have if (process.env.IS_CLIENT === true) {retrun undefined}
add guard to not allow server code in client bundle with s and c wrappers
generic as object with not required fields, undefined itself we can do by "typeof undefined symbol"
Record<string, never> & {}
docs my: ()=>'' favorite: ()=>'' client: ()=>'' fns: ()=>''
.ctx(true, {}) for for assign ctx
own HMR implementation. We need at first wait for server hmr, and send request to client if it needs hmr
combine .route().route()
analyze if there same id points
openapi plugin

override last commit message on each file
choose license
site
prepare start0 selling
npm point0

---

const x = await point.load(async ({ctx}) => ctx.prisma.x.findOne()) // or .exec ?
add to poinets collection generator retrieving .load and .useLoader fns so they can be used in server
.useStaticLoader((props) => ) only server rendering
.useLoader(((props, ...args) => ...), [arg1, arg2], [optinalZodSchema, optinalZodSchema]) (dynamic returns {data,error,loading})
react query plugin wraps .useLoader and provide useQueryLoader and hydration out of the box
.res(({req}) => { req.json() }) || .res(() => [404, {any: "json"}]) || .res(() => [200, {any: "json"}]).page(() => <div />)
.component() has hidden route and can have fetcher
.component, should not recall already called middlewares


