separate useLoader and useQuery
allow pass props to layout and component and clientCtx
rename clientctx to provider
location optional prop and get it from getCurrentLocation router
queryClient optional propa get it from this.getQueryClient

head as argument of .page() it can be string | head | (loaderResult) => string | (loaderResult) => head
.head(head) .head(string) as always shorthand
.head({loading, error, success}) => {title: 'x', ...( error && {title}, ...(loading))}

wrapper
merge .input schemas

chain: const auth = x.chain((role?:string) => x.loader((c) => c.role === role)), myPage.use(auth('manager')), requireData()
→ chain: Point0.chain<TCtx, TData, TClientData>().what().ever()
→ chain: Point0.connection<typeof expoBase | typeof siteBase>().what().ever(); 
? so we need in extract also add base extracctFns
lets add TBase/TRoot, and store there Infer of base, for example after Point0.connection<typeof server>()x.()x.() there will be still typeof server[Infer], wo we can remove TConnectedRootSourcePoint and TRequiredCtx, maye we Need store in Infer also this TRoot, and it can be nested for 5 levels for example

elysia example
auth

expo example, just define all queries and mutations as plugins and use them in site and expo
expo router

like always do general fns
config to forward settings to client and server. jsut {server, clients, generators}
env POINT0_SERVER_PUBLIC_DIR, POINT0_... to override config props
? maybe move config to Point0.extend and Point0.create and extract it from there
cli for simplify commands, like build with bun: point0 build

workspace packages: @point0/core @point0/server @point0/wouter
start0 without mono0

status: .loader(() => [201, <div />]).loader(() => [404, <div />])
? split index.ts code
.disable()
preventSsr
props to clientFns for component, and also to page, layout, clientCtx
queryCache for clients queries
.route(string)
router export function getCurrentLocation()
fix fn prop type for public things, make input first arg, location is optional setting in second arg object
add _ to all clases
basepath replace with cwd
default client query options, default client page query options, default client component query options
not errorComponent, but just error
.isLoading(({queryType, query}) => )
on server when returning ssr loactio replace it with toBasOlcation of requestest origin
.wrapper() → it is like useLayout, so anything with children, But may be then it will be just wrapped in place?
prefetchPageQueryOption as separate settings
server clients do not need root, it will be retrieved from points in eversion create call
FilesGenerator.({routes: Routes()}) then no needed to runtime generation
not TRoute, but TRouteDefinition, and then it can be string, and then no problems at all
page prefetch should be auto, if has App then full, if has no app, then partial
eacho point should have prop .prefetch(boolean) and .ssr(boolean)
layout provides Layout component, That can be just used and it will work, also page provides Page component that will just work, so .route() is needed only for ssr and Router Routes generator
static headsh should be also merged one to another, becouse of blinking
points collection allow to route: undefined, hasLoader undefined we can know on server after loading points
? inputs chema not needed, just input enough. No! Becouse we can have route whch defines part of input, but we can have no input schema. Or Yes! And when we define route we also define input schema, and new input schema should have same keys or more kike route
Router accept points and Routes get pagesTree from Context
remove server code from client
query client nice () => provider safe for ssr
store each point location to normalize errors messages for example in No route or id provided for this point
layout scroll position
on navigate to same page do not prefetch, or at least not show preloading
on navigate on unknown location do window location change
force page to have route
test if layout can have no route
prevent endless loop hmr after code changes
allow router policy prefetch-partial to use by default inside prefetchSuitablePagePoint
find unused code
.loadingMeta() for client async client loaders
? asterisk layout as .layout(true, () => <div/>)
immutable ctx and data on extractFns calls
refactor error0
remove wrapper
before start generate parse existing output file and use it like initial state until success generates new
html template parsing by provided vars
hide methods after .route() called, etc (maybe add pageCanNotBeSetWithoutRote, maybe add deprecation flags), on call any cloient fn or head(() =>) endpoint type is client-middleware and als _pointType
route0 respect origin on match and try two clients with one server
.extendRoute()
remove previous route and id after calling new fns for loaders or ctx, when we use ready point as base
analyze if there same id points or route points
client.create('page').route().loader() → it provides nice helpers
superjson
prefetch on Link hover
make components and pages named in react dev tools
prisma in example
refactor example

test for units
test for server

site & docs https://docusaurus.io/
override last commit message on each file
choose license
prepare start0 selling
prepare subscription
npm @point0

---

const qr = base.lets(z.object({}), () => Response | Data).useQuery({})

? .restinput<T>({params:...,query:...,body:...,headers:...}) or (.params<T>().query().body().headers() or .query(zodScema) or .query(anyOtherSchema), .output({}).status()
? openapi plugin
? persistent zustand store
.isSuitable(() => boolean) to override default route suitable behaviour
example vite-hono
client input
? statefull meta, or better ↓
{...head} as only or second argument in loading and error fns, and as second arg in page fn
.output(z.object({}))
allow not to pass server root at all to server, so client only apps
websocket
pageParamFromInput as fn or path
.onError and combine them and clear them to base on lets
batch query
layout can provide its own 404 page, but it can do it already (not exactl, but can by jsut creating page)

? foxyLoader, clientLoader and clientOnlyLoader, clientLoader extracts on server but not sending as data to client, but on clientAddsTo queryClient special cache, clientOnlyLoader mean it will be shown loader after render
? clientOnlyCtx and clientCtx query key
now it pretty enough

default route of page is its name
? interstallar