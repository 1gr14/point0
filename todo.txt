--------------------------
------ before start0 -----
--------------------------

TEST
assert exists helper
? react-dom/test
Энжин криэйт чисто по массиву поинтов для сервера и клиента через тест инит с комиплеромы выключенным по умолчанию
Наладить экзекьютор, чтобы сам реквесты выдумывал и легко вызывался
Наладить фетч хтмла лёгкий по поинту и инпуту или экзекьют вероятно просто фетчХтмл добавить в поинт
Фетч0 пусть трекает все запросы включая распаршеные по поинтам и ответы → TestFetch
Wfb а хелперы в аргументах вернуть
fix all tests CompilerFile, why it brokes
Изобрести что-то типо глобалЗыс чтобы стал прокси и виндоу андефайнед если в асунк сторе сказано, что он именно сервер, а иначе пусть отдает фейковый виндоу
Энжина должна иметь апплайФетчер
ТестЭнжин как отдельный класс в пакете энжины, там же пусть будет тест бразуер
Дев и Билл тесты сделать в цикле они должны быть одинаковыми
Кли тесты команд с названиями реальных команд, а отдельно тесты на билд также сделать через сам скрипт
vitest check

SUPERSTORE
pointsmanager store in superstore
Инит суперстора в энжине
У инит есть вэлью и таргет, и плоское вэлью, которое при проверке таргет будет определено однозначно
Энв должен также говорить нам клиент фейковый или нет
Суперстор разделить внятно на персистент и темп,
При объявлении можно как-то уточнить сср флаг тут или нет
В симплифай можно передать как объект с готовыми переменными, так и объект с настройками
Не симплифай а мэни
СуперСтор симплифай и передать в него объявленное, он отдаст прокси объект с сеттерами и геттерами
Суперстор на фейковом клиенте должен складывать всё в глобал со своей фейковой припиской, чтобы не мешать другим
Суперстор дефайн это переписать значение по умолчанию, а апсерт, это написать, если не написано
Энв походу должен переехать в поинт0, потому что ему нужен суперстор, или (+) суперстор должен выехать в отдельный пакет
Пусть суперстор дефайн будет отложенным и сохраняется до поры в глобалзыс, при ините уже установятся значения
Суперстор клнфигур позволяет передать туда трансформер, по умолчанию как всегда там суперджейсон
? Квери клиент это отдельный файл в коре, по сути просто ранее задефайленный кусок
? Суперстор тоже должен иметь опцию инит тогда это общая, а иначе инит объект сервер клиент, и он будет значением по умолчанию
? Суперстор можно сделать инстанс и передать его в рут поинт в настройках (убедиться, что это мешает тестам)

REFATROING -1
Энв скоупес хэз, и скоупес массив, эна скруп нэймс
В компилер констс можно нод енв установить андефайнед и он не перепишется
Компилер настройки, прунЭнжинБандлерКонфигОнБилд, таргет фалс, скоуп фалс, мод фалс
Таргет нон для компилера, и при нод тест по умолчанию таргет нон
Индексхтмл в энжине разрешить генерить по указанию тру (или даже если указания не было, то всё равно генерить при сср попытках)
mutation error type should be error0
Из индекс файла импортировать везде только самое важное, а все типы дополнительно экспортировать из тайпс файлика
Убрать локалхост из тэйла тестов, просто путь пусть будет
? А суперджейсон тоже должен быть по-молчанию включён и его просто можно переопределить через опции
× Поинтсы получается нужны только на верхнем уровне в энжине? и олпоинтс менеджер не нужен (если он только на верхнем уровне всё равно получается придётся проходиться по всем поинтсам, и как-то их группировать по скоупам, и мы опять получим 2 сущности)
× Просто пейджес три это отдельный класс и поинтс менеджер хранит сет из этих тришек по скоупу (↑)

ENGINE
Компилер в плагины всегда вставлять на самое первое место
Вайткоефиг и банплагинс разрешить объявлять на верхнем уровне конфига
envVars
envConsts
comiler is part of server and client, and in build pahes it should be getted from it
? Прун он билдпхэйз просто на уровне Хардинга энвы можно задать
Генератор для клиента пусть берёт только иниты пейджи лэйоуты, а для сервера только с серверными лоадерами и пейджи лэйоутв с ссаром
Компилер прямо в энжину можо передать, аможно передать фалс и тогда его там не будет
хмрПорт или желание отсутвия порта не нул, а фалс должен быть
Убедиться что при указании клиентского скопа в энжине сервер не запустится, и запустятся только нужные клиенты, и что они могут обращаться к серверу в другом процессе
В вайт мод и прочие конфиги добавить скоуп, то есть энв кастомер и скоуп, и добавить парс вайт мод, но и прикидывать это как аргументы
Волкером анонимные функции заменять на название PageNewsPost внутри маунтаблов
baseurl get from root point, and remove from engine config
В префетч мод заменить на полиси
auto port getter
generate: {points:path, routes:path}
server should get only its own points, and its already should include all points, so we do not need all-points-manager
? Убрать из энжины необходимость указывать импорт мета файл, мы же и так знаем путь к ней до билда в самом билд скрипте (ох...)
binary, just throw error if some engine distdirs not inside firstlevel server distdir, else just copy them as dirs when do one-file-executable
Путь к энжине не п не е а ф, а лучше посмотреть как у тсконыига называетсяE
compiler options to engine, via ({customer,scope,environment}) => options
fix compiler option name for node_modules prefixes
Генерейт поинтс если ему нужно пусть генерирует через объект с путём и тайп лэйзи или реди
В энжину импорт мета файлнейм автоматом добавлять через компилер
Рантайм тчк билд по ней попробовать вайт отрезать
тест билдера, что он прунит что надо реально удаляет дедкод
Причесать, какими ключами владеет в энжине сама энжина, сервер, клиент
Написать тесты на правильное определение путей после билда в энжине
Рут по умолчанию срц для вайта

INPUT
Просто ввести клиентИнпут и всё будет ок
Не надо инпут схему мержить в одну, просто парсить раз за разом
Проверить расширяемость инпута на юниты
Инпут может принять функцию, или принять просто генерик тогда в тсхема подставится функция
standard schema
Стейдж для сервер штук тоже нужен для порядка
? И тогда появляется серверинпут, который спрунится на клиенте (да норм иметь шаред инпут, пусть в лоадере валидируют, если хотят скрываться)
Инпуту разрешить быть юнионом, и тогда можно при попытке объединить, инпут превратится в функцию
? Вероятно вырезать инпут исходя из того он уже на сервере или ещё на клиенте (ерунда какая-то)

REFACTORING 0
? При ините тест прожекта проводить клинап
Билт воз, билт дефайн бефор, билт дефайн афтер, и не билт а билд (уточнить билт или билд)
Мапер под носсер тоже прунится
Хмр фикс оставить как есть, но по умолчанию везде экстернал функция
В любом случае выносить при хмр фиксе функцию вниз, и помечать дисплейнеймы, и назвать не хмрфикс, а тейл, и делать это всегда без настроек
Деф тчк сервер, а в енв деф тоже скорщённо
В ССР можно передать строку финалайз, тогда будем прогонять ещё разок с финалайзом в конце
__POINT0_GET_SSR_PHASE__
Дать сср только на руты
При повторном использовании компонента можно его просто, то есть если туда переменная была передана, то мы при фиксе хмр просто клонируем этот компонент. Да не, туда даже лучше отдельной функциейдобавлять фикс и всё это назвать ребинд, и вообще не давать это настраивать
e2e tests via code
browser test via running server
hmr tests
bun run test-no-log (trhow error if any .logSomething exists)
displayName dynamic
rmove LazyPointsCollectionRecord if not genreated points use it
Точка инфо на поинте, просто показывает сразу все данные а там уже и имя и тайп
Лоадер стейдж нужен, чтобы все в рядок стряли
query client is special variable in special file, and it can be redefine and get ??? query client can be defined on any root, and will be attached to all requests from this client scope
point0 also separate file, index jsut reexport everything
ssr can be not on root point, but client still can be ssr if it has ssr point, so check ssr by any point has ssr option
layout pruned .layout() transforms to .layout(() => this._layoutLoading)
Если не сср поинт, то прунить ещё для сервера и компоненты до первого сср фалс, а значит нужно при компилинге уже всех парентов находить
allow .ssr() on any point
Разрешить undefined () в тех конструкциях, где мы пруним, чтобы было понятно, что тип андефайнейд возможен (в исплементации под перегрузкой)
html prefetch mode
Все моунтабл компоненты оформленные с асунк функцией автоматически делают префетч мод htmlом
Заменить поинт на поинт ИНСТАНС а то путаница какая-то, и в поинте хранить только тайп и нэйм
ЗАчем вообще нужен сет, пусть будет просто эффектс, а в них уже сет
Вынести из супер стора сериалмзацию, перенести её в сср стор который такойже как кукисстор, и также устанавливается в эффекты
Попробовать только супер стор объединить с индексом
? add ssr to queryKey and if it is false, then do not ssr when meet it (what?)
? На фронте в префетче добавить политику клиентЭндСерверНоСср, чтобы в случае квериклиентДегидрефтедСтейили эвересынг их дофетчить (what?)
Экзекьют на сервере пусть не возвращает статус
Не респонс0 а просто эффектс и эффектс вэлью
? На клиенте в кукисах хранить фром поинт0, и тогда запросы к бану мидлвару можно исходя из кукисов перенаправлять (we already use client url for client, and hmr persist, but for those who use server we can do it)





------
------
------






MIDDLEARE
? low level server available on root points only
available everywhere, but can break anything, work only with final response, do not have access to ctx or data
.middleware(({request,point,publicdir,next}) => {})
remove auto cors for localhost from engine server, and made it via middleware
allow Executor with or without middlewares
Мидлвару можно на любом поинте задать, просто она будет отсекать обращения к другим поинтам

USE/POINT
.use(point)
applies everything that previously was not applied to point that call .use()
but ctx and data just append. like const x = execute(plugin), and the Object.assing(ctx, x.ctx)
so we do not need define if target point data suitable to injectable data/ctx
when we use plugin with loaders we override all previous loaders with its loaders
× ??? we store everything previouse (like extract fns) in root, so we should have getExtractFns() etc, so when we use(point) we only accept it things without it root things, and we shpuld have TScope for all it scopes so we can not use point from diffinetly another scope
!!! Не надо на юз сбрасывать лоадеры, они просто расширят и дата и кткс

USE/PLUGIN
Point0.lets('plugin', 'myPlugin').requireCtx().requireType('root').ctx().loader().clientLoader()... any methods
.use(plugin)
.use([plugin]) to make returned ctx flat
here in plugine we may have .requireCtx(), becouse plugin may be related to some ctx data
cors plugin
Плагин может запровайдить в том числе и рендер штучки, ему ничто не мешает быть внутри функции, которая использует запровайженные штучки (это позволит например целую форму регистрации замутить)

ACTION
add "parse" to options, like parse.input(), parse.body() etc
? and myabe in ctx fns we also need parse, not parsed input
we do not need THeaders as generic, it will be jsut in TSpecial
request0.body.parsed/parse/wasRead/raw/json/text/validate/...
!!! Before openapi endpoint we call only middlewares, and provide ({executeSelf}), when you call executeSelf() then input will be parsed, then will be called all previous loaders and ctx fns, and it will not be pased before
if it is openapi request, then we do not manually parse body for input, else we just provide helpers in loader to get and parse input (search, body, params) from request
Я могу те же самые йиэлд, возврат текста, и возврат форм даты просто опираясь на возвращаемый тип, когда буду делать опенапи поинт, ничего там такого нет
Хеадерс фн должны принимать всё что и обычный лоадер, но может не быть дата и инпута если это опшонс реквест
× .lets('openapi', 'my endpoint', {method: 'get' | 'post' | 'put' | 'delete', route: string, input: z.object({}), output: z.object({}), ... , variants: [] })
! .lets('action', 'myAction', 'get').action({method: 'post', route: '', body: z(), params: z(), search: z(), headers: z(), cookies: z(), output: {like hono}, handler })
× .cookies(z()) (ЗКуки не нужны и зхедеры тоже, это все нужно только опенапишникам)
× .headers(z())
add generics TRoute, THeaders, TCookies to Request0
return type of loaders to response type, yield, etc
TOpenapi, and if exists, then loaders has .loader({openapi}: {openapi: TOpenapi})
if TOpenapi, then it can be query or infiniteQuery or mutation, based on what was provided .query(), .infiniteQuery(), .mutation()
_slug ← name
Под опенапи сделать Парсинг каеристринг через ту штуку чтобы массивы и объекты дали
Опенапи фн а опшинсах имеет селф, который и можно передать в экзекьют, если нужны прежние лоадеры и контекст

LISTENER
.lets('listener', 'mylistener', scopes).client(eventName, {type,set,input,request,point,publicdir}).server(eventName, options).listener()
all root middlewares call on start
all point middlewares call only on points
{onFinish?,onSuccess?,onError?}
? .onError, .onClientError (on any point or root) (after middleware and listener, будет понятно как надо правильно это сделать)

INIT
? root shoul have no loaders, and its ctx fns, should have no access to request, so we can assign root to things like "init" and "mcp"
.lets('init', 'db', {dep1, dep2}).loader().clientLoader(), and rename current TInit to TLoad, it has no context at all, it just return data, here deps will be just added to ctx. Returns {...value,_:{value, client: clientValue, server: serverValue, init}}; 
Депенденсы инита перед словом сервер или клиент
.client .server .engine (so server and cient will be in <App><Initializer>, and engine will be only in engine.init())
So we also need <my.Init>{children}</my.Init> and <Init />, bet
So we need be able to auto init or not, lets add .auto(true | false) by default true
and how cool it be to set init points just in usual ctx of points. Incredible cool! But when we assign it to ctx it gets only server value, I think use should manage it by hmself, just set {...ctx, ...my.server}
Летс иниту всё же придётся прогонять сунковые клиент лоадеры на месте (но он вообще по другому экстрактится, он даже не экстрактится а инициализируется), и всё же
.enabled()
В ините клиента только синхронные функции, иначе это уже провайдер. И инит происходит наэтапе макета то есть сервер даже не видит что там
? Поинтсы провайдить не в моунт, а в инитиалайзер апа (вероятно)
В клиенте вероятно нет клиент и сервер, всё разгуливается просто в функции инит через энвиронмент, надо решить проблему того, что если в ините клиента асунк испорты, чтобы после сср не показывало ловлинги. Вероятно надо просто добавить поле сср, в которое можно добавить строку клиент или но например
Тут короче клиент просто если сунк, то он мапер, а если асунк то уж что поделаешь будет лоадинг на весь сайт

EXPO
expo example
expo router
respect null on baseurl for client, so page like requests to server will be not recpected
check if there is no client port defined if there is no App and indexhtml, so it should not be served
? remove location from mountable components, becouse in expo for example it is very strange property

LAYOUT
.layout(layoutPoint)
.layout('layoutPointName')
? Лэйоуты объявленные с астериском применяются сами по себе в момент генерации поинтов
Если лэйоут или пейдж прокидывае инпут в пропсы, тогнда мы их не берём из парамсов рута
Лэйоут со звёздочкой нужен, а четвёртым аргументом он может передать индекс

REFACTROING 1
route0, error0 → point0?
bin → cli
Он саксес квери и прочее дополнительно надо вызывать при Дип шмип фетре
Разрешить лэйоут передавать через метод, и сделать его автоматически провайдером (просто добавить проверку на инмут лэйоута, если сходится, то и норм)
? Квериклиент добавить в методы рута, и сделать его скоуп зависимым (да норм же иметь глобальный)
refactor types names and order and any points, etc any things. Поправляя эни попробовать отказаться от ифэни в пользу стринг экстендс
В оутпут тайп добавить хтмл
try do EmptyData as {}
remove default generic values it is unsafe, better add AnySomethingType for it
Отрефачить коллекцию поинтов, чтобы там мог быть хоть лэзи, хоть реди рекорд, и они могли как угодно комбинироваться
Вынести из сервер и из клиент в отдельный пакет чтобы мочь применять его в других библиотеках
Разобраться с эррор баундари для фронта
добавить дебаглогеры повсюду, а также на фронт разрешить прокидывать логер, и вообще логгер не часть энжины, а часть поинта

START0
start0 without mono0
refactor Error0: check ConvexError style; base has only cause recursion, everything else is plugins like isFetchError
refactor Meta0: Мета0 это просто асунк стор, который позволяет прокинуть мету в глобальщину, и тогда никакое трио не нужно; Meta0 and meta0 with poor data exists, but we can createMeta0 → MyMeta0, and do const myMeta0 = MyMeta0.extract() and MyMeta0.provide(() => {})
refactor Meta0: Мета всегда с маленькой буквы и есть только методы инит, клон(эти с колбеком создают стор), ассайн, гет (эти меняют стор. И есть отдельная функция криэйтМета0, которая создаст такую же штучку но со своим стором
? refactor Ctx0 → init point enough
refactor Env0: just cool validation helpers
? refactor Logger0: just use as is Logger with global Meta0 provided
unwind and tailwind
https://oxc.rs/docs/guide/usage/linter/config





------
------
------





REFACTROING 2
? Квери клиент тоже в предефайнед, убрать его из самого поинт0 (ну он у нас один на всех, типо как и кукисы)
? on mountables TQueryResultType should be undefined untill final method, or query method, or infiniteQuery method, then point becomes renderMiddleware (whcih problem do we solve?)
walker cache on read and parse with await (promise) memoize until settled
fix what can be .lets from each point type, restrict where lets can be used, like no lets on mountables I think. but can be on queries
Инпутрау только где ющер вводит, иначе инпутрауанноуе
Проверка что все клиентские поинтсы принадлежат серверу
В роут с генерированными роутами добавлять в комментах путь к компоненту, который объявил
В куки добавить стрикт, чтобы если стрикт тру, то не парсить строку трансформером, а ещё лучше {transform: false,true,'auto'}
В суперстор добавить скоуп, чтобы оно не отдавалось в противном случае в сср
Названия не хешировать, а просто нумеровать в экспорте поинтов
Поправить типы в поинтс менеджере, чтобы все коллекции были верные, и не роутед, а нормалайзед (и вообще отрефачить там всё проверить что там нужно)
Вайт конфиг авто, дефаулт, или путь, или импорт, и при билде пруном из энжины это вырезать, в отм числе и бан плагины вырезать из конфига энжины прунером при билде
find TODO in code
throwOnBundlersLengthNot2 → to eslintplugin
? Сделать тюзед для скрытия использованных методов
add point self to loader and to ctx and to loadrCtx options, and everywhere where can be point, add point
on fetch error maybe no json, then if it is thore, do res.text()
normzliae all errors texts everyhere
try catch evertywhere in engine to show real error in real place
store each point location to normalize errors messages for example in No route or id provided for this point
Проверить хмр компонентов лоадинга и врапера
shrink core/index.ts size, by remove code duplicates
Провайдер (вреоятно назовум его как-то иначе, возможно clientBeforeLoader и clientAfterLoader) можно добавить и к странице и к компоненту и к лэйоуту, тогда там мы можем на клиенте сровнуть ошибку например чтобы попасть на компонент ошибки, и в тоже время не иметь состояния загрузки. Установив провайдер его больше нельзя установить, и в то же время мы переходим в рендер стейдж
throw error on Points.create() on cleint with different scope that was before
throw if same name and type in current scope
wouter add to Link match result, so we can use current, active, aprent, exact, etc (location => className)
check bun compiler, and vite compiler creates correct sourcemaps
make components and pages named in react dev tools
fetch/query cancellation, maybe fetchWithCancel()
.getFetchOptions()
allow ssr different html for ErrorComponent stack
loader(() => {}, {cache: false | string})
ССР опцию можно добавлять в квери, чтобы при серверном рендеринге если квери находится, то она все равно не префетчится
? Юз лоадер респонс должен быть сервер респонсом если там такой есть
? Добавить реквест реферера и там указать какому скоупа принадлежал реферер
? remove loaders and request from root. (do not remove, becouse for 'init', 'mcp' we will shake all ctx and loaders already)

COMPILER
! Отрефачит стэйл и прочую ерунду в компилере
! При чтении файла также стейлить все файлы которым он является зависимостью, с указанием таймстемпа, когда происходит чтение
(exprt) => xprt.cnst | xprt.dflt (to valid syntax in places in tests)
? Сср в дефайн энва тоже надо добавить и убедиться, что там невер указав всё не перекроется
? Объединить все траверсы в одно месте, чтобы за раз всё проходить
? Походу надо отдельно прунить для сервера, клиента, и сср. Вот в чём фишка
В изначально не паргеный поинт тоже добавить ошибку
Написать тест на циркулярную зависимость при парсинге поинтов
Убедиться, что компилер не ломает информацию о выведенных позициях, иделает нормальные соурсмапы
walker cache for compiled files by settings and file timestemp
readLimit, parseLimit
walker persist on bun plugin and vite plugin during request, so lets refresh read files by timestamp


TEST
test for units
test for engine
test for e2e (maybe lightpanda)

REFACTORING 3
Move type errors to its own types, so we can reuse them
Replace all node methods with native bun methods, including watcher
analyze why client bundle so big in examples
on request to server for query or mutation which has no serverLoader throw error
? Переделать всё на функции, поинт сделать просто типом, и даже lets пусть будет функцией выносимой
? possibility to subscribe to body chunks read
? ЮзЛоадерРезулт а там уже и куки и дата последняя полученная
? Суперстор тоже в отдельный пакейдж, стобыего могли другие разрабы переиспользовать

CREATE POINT0 APP
cli to create app
Опция криэйт апп поинт0 сразу предлагает купить старт0

REDIRECT
allow redirect by redirect() right inside outer, or via useRedirect()
redirect (as option in loader and clientLoader for page)
redirect as point method which will be applied to both client and server

MCP
.inspect() с положением каждого отдельного компонента
cli list page, cli get page pageName
mcp server for point0, so when it is in dev mode it provides info about all points
mcp server .lets('mcp', 'whatever') or mdx doc. even with loaders. And it becomes available via mcp server, and can be used to autogonerate .curecor rules or event not
mcp server .lets('batchMcp').mcp([{name,description,input,loader?}]) for extrnal libraries like UiHero and all others so we always have fresh docs for each library we use
cli extract point by type and name, so agent can see errors right in terminal
.doc(string) в любой поинт можно добавить
В мдкс для мсп пути к файлам указывать через импорт с макросом, который заменяет их на просто строку полного пути, чтобы не сбивались пути при рефакторе

MDX
! mdx as page point output if meta contains: lets: 'doc' | 'page', route: string, base: import layout or root, loader: ()=>{}, clientLoader: ()=>{}
Мдкс если видит в своей папке или выше файл тчк роут то он файловый роут
Мдкс может в бэйзе просто написать скоуп тчк тайп тчк нэйм
??? .lets('multiPage', '/xxx/:module').modules({routePart1: () => import(./xxx.mdx), routePart2: () => import(./yyy.mdx)}).multiPage(({module})); prefetch needed module on page visit in mount fn

FILEROUTING
Просто > ставить в строку, чтобы файловый роутинг получить
> файлроут, которая автоматически вставляет в себя строку текущего файла по отношению к файлу .файлроут или текущей папке. И к ней даже к строка можно дописать плюсики или как стринглитерал заиспользовать
'>' → will be rewritten to () => '>currentfilepath'
'>+zxc' → will be rewritten () => '>currentfilepath+zxc'
Route0.create('>+zxc') → will be rewritten () => Route0.create('>currentfilepath+zxc')

STATIC
static site generator; 
Префетч мод хтмл, просто тупо грузит новую страницу и открывает
.navigationPolicy('hard' | 'soft') or somehow or maybe not needed;
.static([input1, input2, ...]) then .loader() run on build static phase and not run on prod env; 
or .static(false, [input1, input2, ...]) then loader work like usual loader and not runs on build static phase; 
also allow .static(10, ({take, skip}) => [input1, input2]) or .static(false, 10, ({take, skip}) => [input1, input2])

PRUNER
check if real third-part packages not rhow on client or server
serverOnlyPackages, clientOnlyPackages, throw on build and on runtime
inspect by filepath how this file and in which place come to files via cli command

REALTIME
mySub = root.lets('subscription').input({chatId}).subscription<{eventType: 'newMessage'; text: string} | {eventType: 'newGuest'; name: string}>(throw if not belong to user, return filterFn like ()); mySub.subscribe({chatId}, (event) => handle event); somewhere in server mySub.publish({chatId}, {eventType: 'newMessage', text: 'hello'});
mySub = root.lets('subscription')
.subscribeInput(z.object({chatId: z.string()}))
.sendInput(z.union([z.object({eventType: z.literal('newMessage'), text: z.string()}), z.object({eventType: z.literal('newGuest'), name: z.string()})]))
? .sendKey(z.object({eventType: z.literal('newMessage'), text: z.string()}) | z.object({eventType: z.literal('newGuest'), name: z.string()}))
.subscription(throw if not belong to user, return filterFn like ());
mySub.subscribe({chatId});
mySub.send({chatId}, {eventType: 'newMessage', text: 'hello'});
Сабскрипшен, инпут (при сабскрайбе с клиента), оутпут (при сенде с сервера), в конечной точке вообще ничего не надо писать пустая функция,  все лоадеры и контексты вызовутся при сабскрайбе, весь инпут и хедеры передадутся в серч параметрах, но для лоадеров их превратим в фейковый реквест перед обработкой. На клиенте дать тчк он, и тчк юзОн, на сервере эти штуки будут проигнорированы. Сабскрайб происходит в момент тчк он или юзон и возвращает статус, состояние. И в самом поинте есть тчк онСенд в который передаётся информация о клиенте к которому мы подключаемся, чтобы если что можно было например отключить юзера или ещё чего в таком духе. Нам нужен отдельный клиентСенд и серверСенд
Вебсокет: рум определяет клч рума и в аргументах указано это клиент зовёт, или сервер
!!!
1 messanger. clientSendInput, serverSendInput, clientJoinInput, onClienSend, onServerSend, onClientJoin,
2 Лоадеры и кткс штуки сами вызовутся, но в эти коробки передастся экстрактСелф, и его можно вызвать в любом клиентском колбеке, а кткс там везде это только реквайред кткс
3 гет на поинте это произвольная функция с гетРумИнпут возвращает строку, её результат должен вернуть онСерверСенд и онКлиентСубскрайб, она запровайдится как гетРум в эти методы
4 на клиенте в глобальном стейте поинта мы храним инфу о том, с какими гетРумИнаутами мы подписались, также при подписке мы и на сервере храним все инпуты под которыми мы подписались, и при рассылке это учитываем. 
5 при создании румов надо хранить и ключ и инпут в отдельнйо мапе, чтобы по нему проходиться
- 6 гетРумКей обязателен, если клиентСубскрайбИпут предоставлен и будет рантайм ошибка иначе ... ничего он не обязателен
- 7 месседжФильтерInput дал схему, юзер когда делает x.subscribe({ input, filter: {id: {in:[1,2,3]}} }) и сам фильтр как объект дойдёт до коннекшена, и будет добавлени в мапу {room: [{wsId,filters}]}, каждое сообщение доставляясь проверяется на соответствие фильтрам если они есть и отдаютсяю в формате {meta: {filterHash}, message: {...originalMessage...}} 
8 x.useSubscribe({input, filter, onMessage}, [deps]) при удалении компонента отправляется сообщение на вебсокет о том, что этот фильтр, или эта комната отписалась, 
9 для кипалайва постоянно по чуть-чуть отправляем с клиента ping сообщения
10 гетРумИнпут может вернуть и массив, например гетРумИнпут в онКлиентСубскрайб вернёт массив. clientSubscribeInput:{ids[]}, serverSendInput({id,diff}), onServerSend({input} => return {message: input, mark: input.id}), onClientJoin({input} => return {mark: input.ids.map()=>id.toString()})  // bad // mark({csInput, ssInput} => JSON.stringify(csInput ? csInput.ids.map()=>{id} : {id:ssInput.id})); 
11 .mark(z.object()), so we only store on redis mark hash, not whole mark
???
Client send accept both zod schema, and callback, also client Send, and return types is known messages
кроме рум схемы, ещё и рум оутпут, в неё заходит инпут а выходит то, что выходит, а по умолчанию выходит стрингифаед инпут
И для тех кто готов на медленное соединение позволить месседжПикер, тогда на сервере мы храним весь руминпут в редисе, а далее сервер идёт по всем подпискам и отсылает только туда, кто пикнет
!!!!!!!
Вебсокет то у нас всегда один. То есть по сути это всегда просто субскрипшен. То есть не обязательно клиенту подписываться на поинт, на который он будет отправлять, и наоборот отправлять на который он подписался
это строго должно быть в 1 количестве. вот как рут в 1 количестве, так и вебсокет myWs = .lets('websocket').input().loader().websocket({defaultInput, inputSchema, onOpen: () => { return wsData }}) → это получается попадает в генерик TWsData
x = myWs.lets('messanger').roomSchema(schema).clientSubscribe(input, handler).clientSend(input, handler).serverSend(input, handler).messanger(); handler = ({input}) => ({message: {any}, room: [...roomSchemaInputs]}); TMessage ещё один генерик, но пусть они все будут в TWebsocket там прямо и хранятся в одном месте
x.server.send(input); x.client.send(input); x.client.useSubscribe({ input, room, onMessage })
!!!!!! return data getter by id .liveQuery.initialData<TData>([] if no loaders provider can be setted up);({subscribeToIds: o => o.data.id, onSubscribe: () => {check ability so subscribe to ids, or even maube rooms inputs}, updateItemByIdAndData: <TLiveData>o => o.data}); .liveInfinityQuery({subscribeToIds: o => o.data.pages.flatMap(p => p.item.id), subscribeToNewItems (default = !nextCursor), updateItemByIdAndData: <TLiveData>o => {...o.data.pages.find(o.id), ...o.data}), addItemByIdAndData: <TLiveData>o => [...pages, page]); myLiveQuery.server.send({ id: 1, isNew: boolean, data: {name: 'John'} }); myLiveQuery.useLiveQuery({ids: [1]});
elysia and uWebSocket separate realizations
И вообще либу обёртку для призмы и чего либо ещё сделать, чтобы можно было эту обёртку использовать, для создания и редактирования и запроса из бд с текущим апи призмы, но возвращать нам будут не только ответ на запрос, но и хелперы для лайв квери и тогда любую бд можно превратить в лайв-бд
Лоадер лайв квери должен просто вернуть билдер дризла или объект с роу и информацией о подписках (айдиФилдНейм)
Для дкса просто фильтр пусть будет в мессенджере и он может быть зод схемой, а может быть функцией которую мы застрингифаем и будем использовать при рассылке
Обёртка над дризлом для реальайма на бэке запоминает что надо отправлять, а возвращая на фронт кроме роусов ещё метк, тем самым фронт как раз и заставляет на это подписаться
!!! Когда будем делать провайдер надо учесть, что он будет вызываться внутри юзстейта синхронно, и далее перевязываться, при изменении полученной дата или инпута, но в опшинсы ему мы прокинем сетВэлью, которое должно полностью соответствовать тому, что он же возвращает. Это позволит в этот же провайдер впихнуть любой реалтайм листенер, и менять данные
!!! Лайв метод для обычных кверей. Просто определяет что должно быть от сервера на входе, а внутрифункции как просто этот посыл применить к данным. И допустим вторым аргументом определяем идентификатор комнаты, который и при отправке должен передать сервер, по умолчанию это всегда просто айди, а поведение это всегда просто замена данных 
Просто нужна функция обёртка над любым дб клиентом. ДайвФайнд и лайвФайндМени, там главное понять что юник Кей, и какие трансформации, вот этот результат и должна отдавать лайв квери с сервера: примари Кей, название таблицы, трансформации
!!! В лайквери просто есть лайвлоадео и он возвращает синхронно просто объект с селектом и подселектом полей короче опции для файнд Мени, в следующем лоадере уже будут данные, и так при инициализации мы уже поймём что на это надо подписаться, а клиенту также в хедерах будут переданы инструкции на что ему подписаться через мессенджер

EXAMPLES
example refactor
example auth
example zustand (usual/persistent)

DOCS
Consumer–Source Eversion / Client-Server Eversion
Если какая бяка будет попадать на клиент, надо просто делать импорт дефер

LAUNCH
hide todo.txt
find unused code
license
sematic release + protected branches (main, next)
npm
social pages
video
article
launch
❤️❤️❤️









ELECTRON
somehow wh we have now in client-server, have same ot main-renderer process, via replace fetch implementation
Надо сразу чекнуть электрон, вероятно консумХэндлер достаточно добавить, чтобы поинт просто взял и стал чем угодно, но будто бы тут лучше отдельную разработку для электорна делать, это подождёт

ROUTES/NOT FOUND
we already have in router option to provide 404
allow * in routes so we can define special 404 for specific paths
optional params in routes

REFACTORING 4
.ctx$ do same but totally override ctx
build watch
check why so slow server restart, check wht we can cache on start, like glob results, or compiler results
optimize client input parsing
allow skip client input parsing
Если какие-то квери были заэкстенджены и не были изменены, то при их фетче закладывать в кэш и то, что они фетяанулись, и наоборот. (Да просто хранить сэйм клиент квери, сэйм сервер квери, сэйм комбайнед квери, и фетчить именно по сэйм квери, а не по себе)
При лоаде поинта в поинтс менеджер глобальном в клиенте помечать его как лоадед с верными типами, и добавить в поинтс менеджере геттер верный
bunup
oxc lint
? Избавиться вообще от летс, и пусть если летс поинта нет, то сама же функция пейдж работает как инициатор. То есть логично что рут и бэйз возвращают на самом деле билдер и из него торчит изначальный поинт и все изначальные фуночки
? pageParamFromInput as fn or path
? on fetch and prefetch if extractor exists in SuperStore, then do not do fetch, but do extractor.extract
? after build validate if dist paths exists, like engine.validate()
? TScope, TType, TName (зачем это надо?)
? И синхронная какая-то клиентская штука, которая будет вызвана до отправки запроса или показа компонента (это уже врапер у нас есть, там можно что угодно в любом стиле проверить)
? simplify renderer
? router export function getCurrentLocation
? use arrays in points files (will be harder to type it, do not do it)
? Пропсы пусть дополнительно генериком прикидывается перед вызовом функции компонента/loading/error с проверкой не стали ли они уже предыдущих
? Пририписки unsafe заменить на safe в клиент сервере и поправить прунер, да вообще не нужен сейф (да нужны они, а вдруг код иполняется в месте где реально клиент и сервер исполняют)
? В сервер экстрактор добавить флаг для пропуска добавления квери в дегидрейтед стейт, заодно убедиться что при экстракте то что квери попадает, а что не квери не попадает 
? Ну и добавить тогда уж тасунк чтобы знать есть ли асунк лоадеры
? .lazy(), а зачем это нужно, можно просто импортнуть нужные страницы на нужные страницы, и они сами сбандлятся как надо

SUPERSTORE
rethink SuperStore, when we understand if it is needed at all
? super store if calling .set() or .get() outside of eversion run, then store this values to queue of pendingSets: [], and in start of eversion run call all this pending sets with warnings

AXIOS
.axios(provide axios instance) .axios(false), .axios(true)
onUploadProgress, onDownloadProgress

.robotsTxt(take, skip)

.output(z.object({}))

??? .loader({listpreviw: () => , () => {listview: () =?}})

example monorepo

batchQuery, batchPrefetch, batchExecute

WALKER/COMPILER
Отрефачить Волкер, пусть просто при коллекционировании поинтов, записывает всё что знает статически, а когда придёт компилер, он просто обращается к Волкеру за уже имеющейся информацией о поинтах и сможет прунить мгновенно
Генератор может и должен хранить кеш в бд, чтобы когда в деве компилер пытался понять как прунить, то он бы сверялся с этим кешем
Волкер генератор прунер кеш прямо контент файла, если он не менялся, можно инпут хешировать и с таким именем файлик и создавать, и подчищать после генерации

CONFIG
config object for point, it should be even mutatable, but type defined only at root point, and it is accessible in any point method
И тогда уж в рут можно добавить сеттингс дефенишен, который можно читать внутри любых лоадеров, и переопределять статически в любых поинтах
И даже можно дианмически, принимая на вход в функцию поинт, реквест, инпут, просто запускаем сбор настроек перед прогонкой поинта, то есть все лоадеры и контексты идут после определения сеттингов
config({}), clientConfig({})
and in loader settings is immutable

EXTENSIONS
И добавить экстеншены, они дадутся последним генериком и могут добавлять методы реди поинтам, методы мидл поинтам, добавлять экстракт фн ситуации

NODE.JS
polyfill

Добавить юзМутейшен и фетч и юзКвери прямо внутрь компонента

Позже сделать летс тчк пейдж и прочие с объектным стилем и еслинт биом плагином для валидации порядка ключей

? А потом и девопс штуки Аля летс ресурс, с такими же зависимостями как у инит CDKTF. Но пусть это делается через экстеншен, который позволяет доопределить всё включая опции энжины, кли команды, летс поинт тайпы, и прочее

Разрешить летс первым аргументом передать что угодно типо свой билдер. Так мы можем и ресурсы тераформа создать, и мессенджер кастомный

При создании рута можно определить тип соурса, по сути это лишь знать реквест0 подменить и респонс0 и фетч

? Рантайм сделать плгинизрованным как компилер, и позволить создавать любые поинты

? Фетчер отдельный класс, который точно также создаётся через криэйт, онКлиентСендРеквест, онКлиентРесивРеквест, онСерверРесивРеквест, онСерверСендРеспонс. Этот же фетчер является генериком и может переопределить реквест и респонс и эффектс полностью. Но это потом, сейчас просто подменяем нативный фетч через кастомфетч
























-----------------
------ ??? ------
-----------------

? remove allpointsmanager, and fix relations points-mamager and all-points-manager and fetch and executor
? Не нужен алл поинтс менеджер, у нас в сервере итак все поинтсы. А для клиента пусть называется просто пейджес три и туда попадают только лэйоуты и страницы
? InputRaw → InputOriginal
? Remove inputOriginal from options of loader and ctx
? В фетч и серв назвать аргумент просто кткс

? При использовании икс квери а другом скоупе мы всё рут икс квери можем заменить на рут нашего скоупа, главное при летсе от Рута не Рута не передавать экстенд ФНС, а брать их в момент экзекьбта из рута запрашивающего скоупа, и тогда уж ну точно у Рута не должно быть дата лоадеров и кткс сеттеров... (да у нас есть разруливатель кода исходя из запускаемого скоупа, хватит всем и того, что они что надо прокинут в х рут)

LETS NOT DO IT
? То что не имеет смысла чейнить, пусть будет третьим аргументом летс, там просто объект со всеми настройками, даже инпут логично иметь там, а вот лоадеры и ктксеры пусть будут на месте
? Если в летс опциях уже названа конечная точка типо пейдж, то это финальный поинт
? И логично сразу с тестами волкера сделать

! we just need own Messanger based on usual websocketsm it is enough
(convex is just real-time helper (.lets('subscribe')), nothing special. And it is big combiner, which is bad)
? example convex (preloadQuery in loader and store data there, then usePreloadedQuery)
? just convexQuery and convexMutation
? convex as @point0/convex, .convex(await import('@point0/convex')), and then we just use convex queries, not react queries
? extendsion, that extends Point0, and Engine, so we can add .lets('convexQuery') for example
? example convex (preloadQuery in loader and store data there, then usePreloadedQuery)
? example convex (I can generate convex dir fully, and just add .lets('convexQuery'), .convexQuery() and here I have just my input as args, and handler fully generated via previous loaders and ctxs)

? server build "dist/server/combined". So after build clients server part, when we build server we replace in engine string paths with import paths
? .attachable(), like shared, but will not automatically attach to root, but can be attached manually
? route0 respect origin on match and try two clients with o99ne server
? html template parsing by provided vars via ejs
? immutable ctx and data on serverExtractActions calls
? asterisk layout as .layout(true, () => <div/>)
? add engine client options serveByServer: boolean default true
prefetch type not only prefetchPageDehydratedState, but also prefetchPageSuperStore becouse there can be zustand or what else

convex example

? interstallar

? Можно для инита, опенапи, реалтайма, использовать генерик спешила, и туда засовывать уже более подробный тип
? Реквайред кткс надо вынести просто в криэйт и не выделываться
? × Если нет плагинов для клиента и клиент всего один, тогда и запускать его будем на месте. Но когда мы захотим прунить по скоупу, окажется, что так не получается, и надо юзеру руками плагины вписывать

Клиенту по сути принадлежат только поинты страниц и лэйоутов, все остальные (квери, провайдеры) могут принадлежать иксу который как бы нейтрален. Если кверя нужна для страницы пусть используют юз перед созданием страницы. Это касается только моментов с двумя клиентами. Короче пусть будет просто поинт криэйт, не надо сервер не сервер

? Добитьсябы того, чтобы энжина сводилось к тому, что весь сервер определяется тем что он импортируется в свойство сервер, а клиенты импортируются в свойство клиентс, и даже пабликдиры

? Так если клиентлоадер сунк, то вот и всё, то чего он лоадинг там выдумывает, пусть прямо статично свой кэш меняет, а изначально его дата в юз Стейт положим

? engine server onFetch hook, onAfterPublicdirFetch hook

? .query and .mutation allow pass last loader right in mutation and query methods as first argument, and fix compiler to work with it

NOT ELYSIA, BUT FULLSTACK FRAMEWORK
??? remove TResponse and TClientResponse should have our special response type with generics
??? in loaders options should never be usual response
??? there always should be our special response with all helpers (cookies, status, headers, etc) https://chatgpt.com/c/69451dc4-a828-8332-b5c5-82011bfca0d2
??? reponse.clone(overrides kind, body) should be returned but may be returned a usal response also
??? afer return from loader response you can not create more .loader or .ctxLoader, but in client loader you can as many as you want
??? куки вынести в лоадер отдельно и сделать схему для их типизации, которая будет пруниться на клиенте
??? и тоже самое для хедеров, просто .headers(z.object({})) и .cookies(z.object({}))
??? И полюбому хедеры и куки будут наложены на новый респонс
...we do not fight with elysia, ay of it route or routes can be defined, lets just later make extensions and add .elysiaLoader, or maybe made it core functionality

× .reqHeaders<T>(z.object({})) // request headers // shake on client → just .header()
× .resHeaders<T>(z.object({})) // response headers // shake on client → why? we just return it from loaders
× .cookies<T>(z...) // request cookies // shake on client → why do we ever need it, just parse it in loader and add needed to ctx (it is optional, but possible)
× .headers(z.object({})) // request headers → we do not need it, jsut parse in loader and add to ctx (it is optional, but possible)
× Own req and res wrapper around original request → when openapi, we will have additional "openapi" option in loaders with all stuff
× in loader return [status, data, res.headers.set()] → not needed in final response header may be any becouse of any connection problem
× return res.build(body, {headers, status, statusText}), return res.build(data, {headers, status, statusText}) → no, becouse it is same as new Response()

? request is not part of required ctx, it is part of loader options always, it is requred on fetch, and not needed on serve
? cookies are shared and part of loader options always. How to sync: https://chatgpt.com/c/69456768-4cb8-832e-8a41-2a9c66dc2fbb
? response.headers.set() → PendingResponse (we just need it for header in fact, all of them will be applied to Response, if loader return Response, then its header will override previous ones)
? request is still usual request
? cookies.set().clear().get() and some spicific
? there is no previously returned response in server loader options, becouse if on return response, then it is clientMiddleware
? there is response in client loader if server loader exists in all steps, after return response from client it is renderMiddleware
? redirect as something that should be returned and provides there in loader options only for pages and layouts
? Реквест тоже свой врапер, айпи добавим, оригинал, инпут рау, кукисы
? В реквесте нашем пусть будет обычный рекорд хедеров, кук без всяких вспомогаек, а когда опенапи будет, тогда оно ещё и типизированное окажется
? А респонс стрёмный, реально может лучше сет (статус, хедер, куки)