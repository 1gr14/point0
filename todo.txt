
head fn and cleintLoader it is clientExtendFns
const clientCtx = .clientLoader, after call do not allow usual loader another, middleware-client
<ClientCtxProvider loader={clientCtx} />
clientCtx.useClientCtx

chain: #chain().plugin() , Point0.use(), requireData(), TRequiredData

auth

points file generator: eversion/generator.ts, have it as standalone and also imported in entry-server.ts to have watch mode on dev

.component(() => </div>) return usual ReactElemnt with property "loader" so on server rendering we can see that he has it and call its loaders having cahe of previously called extendFns
.useLoad|.exec(((props, ...args) => ...), [arg1, arg2], [optinalZodSchema, optinalZodSchema]) (dynamic returns {data,error,loading})
on client build remove server code, just force to rebuild @point0/core package and there have if (process.env.IS_CLIENT === true) {retrun undefined}

example expo
expo router helper

example elysia

config to forward settings to client and server
env POINT0_SERVER_PUBLIC_DIR, POINT0_... to override config props
? maybe move config to Point0.extend and Point0.create and extract it from there
cli for simplify commands, like build with bun: point0 build

workspace packages
start0

.base() set _pointType = 'connected-base' | 'source-base'
status: .loader(() => [201, <div />]).loader(() => [404, <div />])
? split index.ts code
.disable()
.route(string)
store each point location to normalize errors messages for example in No route or id provided for this point
layout scroll position
fix head title blinking
not json but query and mutation, and if it is mutation the on extract do not add it to hydratedState
do not loose state on hmr
force layout and page to have route
prevent endless loop hmr after code changes
find unused code
asterisk layout as .layout(true, () => <div/>)
immutable ctx and data on extendFns calls
refactor route0
refactor error0
html template parsing by provided vars
hide methods after .route() called, etc (maybe add pageCanNotBeSetWithoutRote, maybe add deprecation flags), on call any cloient fn or head(() =>) endpoint type is client-middleware and als _pointType
route0 respect origin on match and try two clients with one server
.extendRoute()
remove previous route and id after calling new fns for loaders or ctx, when we use ready point as base
analyze if there same id points or route points
superjson
prefetch on Link hover
make components and pages named in react dev tools

test for units
test for server

override last commit message on each file
choose license
site
prepare start0 selling
npm @point0

---

? .restinput<T>({params:...,query:...,body:...,headers:...}) or (.params<T>().query().body().headers() or .query(zodScema) or .query(anyOtherSchema), .output({}).status()
? openapi plugin
? persistent zustand store
.isSuitable(() => boolean) to override default route suitable behaviour
example vite-hono
websocket
batch query