wrapper for bun helpers
example idea, example idea new
test for ready server
example general
not PagesCollection, but PagesCollection
remove 0 suffix everywhere
just page0, first generic not Page0 but _Infer (or just _) holder or ctx itself
render fns to another module
.end
layouts

error handling
Error0

.clientCtx
.clientLoader, after call do not allow usual loader

const {ctx, data, meta} = await somePage.extract({ctx: currentCtx, data: currentData, meta: currentMeta}) // inside another loaders or ctxFns
const {data, meta, res} = await somePage.fetch({ id: 123 }) // where res is component or json depends on this page .env response
so we need base path setted in routes or page sttings itself
.component, should not recall already called middlewares
? .require(<>)

? .useLoader ? hmr ok ?

htmlRewriter fall back on no bun
hydrate react query and others
.before, .after
<Router />
client only page
client only routing
server only page so it is usual backend route
ensure layout not rerender, and scroll position ok
<Link />
hide methods after .route() called, etc
meta, title
.plugin, .use(plugin)
html template parsing
adapters, bun, and importRenderer inside 
serve public dir
auto find pages, sort by no params to params routes, save to file
remove 0 from suffixes where it needed
.expose
.component() for compontnts returns React.FC, and .render() for usual pages, on render page should find components inside
.end((req) => { req.json() })
.end(() => [404, <div />])
.end(() => [404, <div />])
.end(() => [404, {any: "json"}])
.json(() => [200, {any: "json"}]).end(() => <div />)


example bun
example vite
example elysia
example auth
example expo

npm @devp0nt/page0
superjson

---

await import inside .exposeCtx fns
docs my: ()=>'' favorite: ()=>'' client: ()=>'' fns: ()=>''